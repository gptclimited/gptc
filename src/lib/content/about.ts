/**
 * @deprecated Import from `@/config/about` instead.
 * Re-exported for backward compatibility.
 */
export { aboutConfig, contactConfig } from "@/config/about";

import { aboutConfig, contactConfig } from "@/config/about";

export const aboutContent = aboutConfig;
export const officeInfo = contactConfig.officeInfo;
export const mapEmbedUrl = contactConfig.mapEmbedUrl;
export const mapDirectionsUrl = contactConfig.mapDirectionsUrl;
