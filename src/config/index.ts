/**
 * Site content configuration.
 *
 * Edit files in this folder to update copy, statistics, images, and CTAs
 * without touching component code. Images live in `/public`; reference them
 * via paths in `images.ts`.
 */
export { aboutConfig, contactConfig } from "./about";
export { homeConfig } from "./home";
export { siteImages } from "./images";
export type * from "./types";
